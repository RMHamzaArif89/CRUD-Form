<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Detail</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container{
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(145deg,blueviolet,blue);
        }
        .user-detail{
        display: flex;
        align-items: center;
        flex-direction: column;
        justify-content: space-between;
        width: 35%;
        height: 70%;
        background:rgba(13, 130, 169, 0.876);
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);

        }
        .img{
            width: 100%;
            height: 60%;
        
        }
        .name{
            font-size: 1.6rem;
            color: white;
            font-weight: bolder;
        }
        .comment{
            font-size: 1.4rem;
            color: white;
            font-weight: bolder;
        }
        .review{
            font-size: 1.4rem;
            color:lightcoral;
            font-weight: bolder;
        }
        .rating{
            font-size: 1.4rem;
            color: red;
            font-weight: bolder;
        }
        .change{
            display: flex;
            gap: 1.4rem;
        }
        .edit{
            padding: 8px;
            border-radius: 8px;
            background:green;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
        }
        .delete{
            padding: 8px;
            border-radius: 8px;
            background:red;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="user-detail">
        <img src="/<%=Data.img%>" alt="" class="img">
        <div class="name">Name: <%=Data.name%></div>
        <div class="comment">Comment: <%=Data.comment%></div>
        <div class="review">Review: <%=Data.review%></div>
        <div class="rating">Ratting: 10/<%=Data.ratings%></div>
        <div class="change">
            <div class="edit"><a href="/edit/<%=Data._id%>">Edit</a></div>
            <div class="delete"><a href="/delete/<%=Data._id%>">Delete</a></div>
        </div>

        </div>
    </div>
</body>
</html>