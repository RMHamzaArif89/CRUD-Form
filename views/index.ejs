<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
    <style>
        .container{
            width: 100%;
            min-height: 100vh;
            max-width: auto;
            display: flex;
            flex-wrap: wrap;
            gap: 2%;
            justify-content: space-around;
        }
        .card{
            width: 240px;
            height: 300px;
            margin: 2% 0;
            background: linear-gradient(145deg,lightblue,lightgreen);
            display: flex;
            align-items: center;
            flex-direction: column;
            justify-content: space-between;
        }
        .card .img{
            width: 100%;
            height: 60%;

        }
        .name{
            font-size: 2.4rem;
            color: blueviolet;
            font-family: serif;
            font-weight: bold;
        }
        .review{
            font-size: 1.3rem;
            color: black;
            font-family: sans-serif;
        }
        .ratting{
            font-size: 2rem;
            color:red;
            font-family: serif;
            font-weight: bolder;
            text-align: center;
        }
        .detail{
            padding: 15px;
            background: blueviolet;
            border-radius: 8px;
            text-align: center;
            color: wheat;
            border: 1px solid white;
            font-size: 1.3rem;
            font-family: serif;
        }

    </style>
</head>
<body>
    <div class="container">
      <%Data.forEach((val)=>{%>
        <div class="card">
            <img src="/upload/<%=val.img%>" alt="" class="img">
            <div class="name"><%=val.name%></div>
            <div class="review"><%=val.review%></div>
            <div class="ratting">10/<%=val.rating%></div>
            <div class="detail">Details</div>
            
        </div>


      <%})%>
    </div>
</body>
</html>